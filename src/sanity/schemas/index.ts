import { AboutSchema, brandSchema, latstVideoSchema, testimonialSchema, VideoSchema } from "./videoSchemas";

const schemas = [VideoSchema, AboutSchema, latstVideoSchema, testimonialSchema, brandSchema];

export default schemas;
